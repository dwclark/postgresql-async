package db.postgresql.async;

public enum Direction {
    NEXT,
    PRIOR,
    FIRST,
    LAST,
    ABSOLUTE,
    RELATIVE,
    ALL,
    FORWARD,
    FORWARD_ALL,
    BACKWARD,
    BACKWARD_ALL,
    CLOSE;
}
